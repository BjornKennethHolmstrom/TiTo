// package.json
{
    "name": "tito-time-tracker",
    "version": "1.10.0",
    "scripts": {
        "build": "rollup -c",
        "dev": "rollup -c -w"
    },
    "devDependencies": {
        "rollup": "^4.9.1"
    }
}
